  <% if !article.image_link.empty? %>
    <% @image = article.image_link %>
  <% else %>
    <% @image = article.image_upload.url %>
  <% end %>
<div class="thumbnail thumbnail_w_h_news_container" data-example-id="simple-thumbnails">
        <div class="thumbnail thumbnail_w_h_news">
          <%= link_to (image_tag @image), article_store_article_path(article.article_store, article) %>
        </div>
        <div class="caption">
            <%= link_to article.name, article_store_article_path(article.article_store, article)%>
        </div>
        <div >
        <p >
            <%= article.content.truncate_words(200).html_safe%>
            <%= link_to "Chi tiết", article_store_article_path(article.article_store, article), class: "news_detail"%>
        </p>
      </div>
      <div class="small_info_time">
         <%= article.created_at.to_date %>
        </div>
  </div>
